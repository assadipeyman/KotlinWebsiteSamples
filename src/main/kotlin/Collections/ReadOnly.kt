fun main() {
    val list = listOf("a", "b", "c")
    val map = mapOf("a" to 1, "b" to 2, "c" to 3)

    //list.add  -> we don't have Add method on read only List r Map

    println(list)
    println(map)
}